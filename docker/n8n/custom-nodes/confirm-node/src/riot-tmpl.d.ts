declare module '@n8n_io/riot-tmpl' {
    export interface ReturnValue {
        // Si tu connais la structure de ReturnValue, définis-la ici
        value: any;
    }

    export function someFunction(): ReturnValue;  // Ajoute d'autres fonctions si nécessaire
}